<div class="container">
  <strong><h1>Lieu actuel &nbsp;<div class="black-title">|</div><div class="grey-title">&nbsp;OSAKA, Japon </div></h1></strong>
  <div class="margin">
    <%= render 'shared/search' %>
    <%= render 'shared/navbar-search' %>
    <%= render 'shared/menuburger' %>


    <div class="big-card">
      <img src="https://res.cloudinary.com/geraldinefd/image/upload/v1575819172/Kitsune-Udon-w600_rfwl89.jpg" />

          <h2 class="suggestion" >Suggestion</h2>
      <div class="big-card-infos">

        <div class= "streamer">
        </div>
        <div>
          <p>Kitsune Udon</p>
        </div>
      </div>
    </div>

    <div class="une">À la une... </div>
    <div class="box">
      <% @specialties.each do |specialty| %>
      <div class="card-category" style="background-image: url('<%= cl_image_path specialty.photo, height: 300, width: 400, crop: :fill %>')">
        <div class="specialty-name"><%= link_to specialty.name, specialty_path(specialty), class: 'card-link' %>
        </div>
        <i class="far fa-heart favorites <%= current_user.favorited?(specialty) ? "liked" : "" %>" data-id="<%= specialty.id %>"></i>
      </div>
      <% end %>
    </div>
  </div>
</div>
